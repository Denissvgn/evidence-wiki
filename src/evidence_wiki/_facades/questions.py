"""``ws.questions`` -- questions, their claims, and their answer pages.

Skeleton only. **Filled by U10**. Add methods here rather than in a shared
module: U10 works alongside units that own sibling namespaces.
"""

from __future__ import annotations

from ._base import Namespace


class QuestionsNamespace(Namespace):
    """Question operations for the owning workspace. No operations yet."""
