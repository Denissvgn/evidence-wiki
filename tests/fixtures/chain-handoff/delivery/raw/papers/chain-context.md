# Chain Handoff Context

This synthetic markdown source describes why the handoff chain needs a
deterministic reference fixture. It uses only reserved example.org provenance
and does not represent real research data.
