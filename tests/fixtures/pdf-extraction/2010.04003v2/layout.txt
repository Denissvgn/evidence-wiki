SYNTHETIC LAYOUT FIXTURE - NOT SOURCE TEXT
TITLE: Context Learning Parser Exercise
AUTHORS: Alex Example | Bailey Example
REFERENCE LABEL: 2010.04003v2

COLUMN 1: Synthetic layout column for context-conditioned prediction. The text is project-authored and associated with 2010.04003v2 only as a stable regression label. It exercises column boundaries, captions, repeated headers, and page markers.
COLUMN 2: Synthetic layout column for context-conditioned prediction. The text is project-authored and associated with 2010.04003v2 only as a stable regression label. It exercises column boundaries, captions, repeated headers, and page markers.
COLUMN 3: Synthetic layout column for context-conditioned prediction. The text is project-authored and associated with 2010.04003v2 only as a stable regression label. It exercises column boundaries, captions, repeated headers, and page markers.
COLUMN 4: Synthetic layout column for context-conditioned prediction. The text is project-authored and associated with 2010.04003v2 only as a stable regression label. It exercises column boundaries, captions, repeated headers, and page markers.
COLUMN 5: Synthetic layout column for context-conditioned prediction. The text is project-authored and associated with 2010.04003v2 only as a stable regression label. It exercises column boundaries, captions, repeated headers, and page markers.
COLUMN 6: Synthetic layout column for context-conditioned prediction. The text is project-authored and associated with 2010.04003v2 only as a stable regression label. It exercises column boundaries, captions, repeated headers, and page markers.
COLUMN 7: Synthetic layout column for context-conditioned prediction. The text is project-authored and associated with 2010.04003v2 only as a stable regression label. It exercises column boundaries, captions, repeated headers, and page markers.
COLUMN 8: Synthetic layout column for context-conditioned prediction. The text is project-authored and associated with 2010.04003v2 only as a stable regression label. It exercises column boundaries, captions, repeated headers, and page markers.

Figure 1: Synthetic parser pipeline with inert placeholder boxes.
Table 1: stage | expected result
input | retained
layout marker | classified
output | deterministic
