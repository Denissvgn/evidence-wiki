# Claude Agent Instructions

`AGENTS.md` is the canonical instruction file for this research workspace.

Claude-compatible agents should follow:

- `AGENTS.md` for operating rules and workflows,
- `research.yml` for configuration,
- `index.md` for wiki navigation,
- `log.md` for append-only activity history.

Do not duplicate or diverge from `AGENTS.md`.
